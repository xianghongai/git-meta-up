# Changesets

Run `pnpm changeset` to describe a change to `git-meta-up` and pick the semver bump. The generated Markdown file lives here until a release collects it into `CHANGELOG.md` and bumps the version.

The playground is private and is never versioned or published.
